# WTF Guru/Trainer — root env sample (see token_server/.env.example for the server)
# Flutter apps read TOKEN_URL via --dart-define (defaults to http://10.0.2.2:3000 for Android emulator)
TOKEN_URL=http://10.0.2.2:3000
